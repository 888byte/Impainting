# Physics-Informed Soft Constraints ??

## ????

?????????????? NPZ schema ???????? RGB + bridge + diffusion ??????????? physics-informed soft constraints?

1. `Spec-Color Consistency`
2. `Parent / Augmentation Consistency`
3. `Damage Score Constraint`

????????? loss ???????????????`physics.enable=false` ??????????????????

## 1. Spec-Color Consistency

???? bridge ??? pseudo spectral condition ??????????????????????????????

?????

- ???? `spec_color_head`???????????? `pseudo_cond`?
- `pseudo_cond` ??????? bridge ???`cond_post` ???????? `cond_pred`?
- `spec_color_head(pseudo_cond)` ?????? `x0[:,0,:]`?
- ????? `x0_pred[:,0,:].detach()` ???????????????????

???????????? Raman/XRD??????? bridge ????????? pseudo spectral condition?

## 2. Parent / Augmentation Consistency

????? many-RGB-to-one-spectrum ????RGB ???????? posterior / pseudo spectrum ??????

?????

- ???? posterior logits ? posterior ??????????? posterior??????? pseudo spectral latent?
- `L_aug_consistency` ??? `augmentation_parent_id`?
- `L_parent_consistency` ? `parent_consistency_level` ???? batch ??? group ???
- `auto` ?????`spectral_parent_id > sequence_parent_id > side`?
- ?? `side` ?????????????? parent??????? `side` ?????? `side_consistency_scale`?
- ?? batch ????? group?? group size < 2??? loss ???? 0?

?????????? schema?????????????? sidecar / runtime metadata ???????????????????????????

## 3. Damage Score Constraint

???????????????????????????????????????

?????

- ???? `damage_head`?????? `[zc, pseudo_cond]`??? `pseudo_cond` ???? `zc`?
- ???? batch ??????? `sequence_parent_id` ? `t` ??????
  - `L_damage_mono`
  - `L_damage_smooth`
- ???????????? `damage_requires_order=true`????????? 0?

????????????? Raman/XRD?????? metadata ?????????

## ??? posterior / prototype / retrieval / gate ?????

- ????????? posterior / prototype / retrieval / gate ????
- `Spec-Color Consistency` ?????? `pseudo_cond`?
- `Parent / Augmentation Consistency` ?????? posterior logits / posterior latent??? bridge ????
- `Damage Score Constraint` ????????????????????
- ? `low_confidence_skip_physics=true` ???? physics losses ??? `detach` ?? confidence ???????????????????

## ????

?????? `physics` ????

```json
{
  "physics": {
    "enable": false,
    "use_cycle_model": "auto",
    "use_spec_color_consistency": false,
    "use_parent_consistency": false,
    "use_aug_consistency": false,
    "use_damage_constraint": false,
    "lambda_spec_color": 0.1,
    "lambda_spec_pred_consistency": 0.0,
    "lambda_parent_consistency": 0.05,
    "lambda_aug_consistency": 0.05,
    "lambda_damage_mono": 0.01,
    "lambda_damage_smooth": 0.01,
    "damage_requires_order": true,
    "parent_consistency_level": "auto",
    "side_consistency_scale": 0.25,
    "color_consistency_space": "lab",
    "low_confidence_skip_physics": true
  }
}
```

?????

- `physics.enable=false`?? cycle ????????????
- `physics.enable=true` ? `use_cycle_model="auto"`????? `FadingForwardModelLab` ????
- ? checkpoint ???? head ???????????????????????? warning ???????????????????

## ???????

??????????? Raman/XRD ???????????????

- `posterior_entropy`
- `confidence_bridge`
- `spec_color_agreement`
- `damage_score`

????

1. ??????? physics ???
2. checkpoint ????? head ??

?????????????? warning ??????

## ????

- `Spec-Color Consistency` ???????????????????? Lab ???
- `Parent / Augmentation Consistency` ?? batch ????? group ??????
- `Damage Score Constraint` ????? metadata???????????????????????
- ???????????????????????????? bridge / diffusion ???
