# ------------- 批量排除所有子项目中的「大文件夹」（无论子项目在第几层）-------------
# 匹配所有子项目中的 data/ 文件夹（存放数据集，多数量/大体积）
**/data/
!**/inpainting/data/*
!**/inpainting/data/
# 匹配所有子项目中的 checkpoints/ 文件夹（存放权重文件）
**/checkpoints/
# 匹配所有子项目中的 logs/ 文件夹（存放大量日志）
**/logs/
# 匹配所有子项目中的 weights/ 文件夹（另一种权重存放目录）
**/weights/
# 匹配所有子项目中的 temp/ 临时文件夹
**/temp/
**/ckpt/
**/experiments/
**/debug_color_guidance/

dataset/
MuralDH/



# ------------- 批量排除所有子项目中的「大文件后缀」（无论子项目在第几层）-------------
# 模型权重文件（常见格式，全覆盖）
**/*.pth
**/*.bin
**/*.ckpt
**/*.pt
**/*.h5
# 大数据文件
**/*.npz
**/*.npy
**/*.csv
**/*.mat
# 压缩包文件
**/*.zip
**/*.tar.gz
**/*.7z
# 可执行文件/缓存文件
**/*.exe
**/*.pyc
**/__pycache__/

**/Val_Dataset/

**.state
**.log

# ------------- 可选：排除所有子项目自身的.git目录（避免主仓库嵌套Git）-------------
# 若你不想把每个子项目的Git历史上传到主仓库，添加此条
**/.git/
StrDiffusion+e00/train_brush/texture/config/inpainting/log
